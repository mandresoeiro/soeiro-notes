# ⚙️ Views para Processamento HTTP

## 🧩 `TemplateResponseMixin`

Renderiza usando `TemplateResponse` (com contexto atrasado).

## 🔄 `ContextMixin`

Adiciona dados ao contexto via `get_context_data()`.

## 📬 `ProcessFormView`

Base para lógica `GET`/`POST` separada ao lidar com formulários.
