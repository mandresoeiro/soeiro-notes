# 📝 Mixins para Formulários e Models

## 📄 `FormMixin`

Base para `form_valid()` e `form_invalid()`.

## 🧱 `ModelFormMixin`

Liga automaticamente um form ao model.

## 🆕 `CreateModelMixin`

Usado para criar objetos via formulário.

## ✏️ `UpdateModelMixin`

Usado para editar objetos via formulário.
